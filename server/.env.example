# Backend Server Environment Variables
# Copy this file to server/.env and update with production values

# Admin Authentication Credentials
# Use secure passwords in production
ADMIN_EMAIL=admin@aseer.health.sa
ADMIN_PASSWORD=Aseer@2026

# JWT Configuration
# Generate a random secure string for production
JWT_SECRET=dev-secret-change-in-production

# Database Configuration
DATABASE_URL=postgresql://user:password@localhost:5432/aseer_health

# Node Environment
NODE_ENV=development
PORT=3001
